---
import BaseLayout from '../../layouts/BaseLayout.astro';

const title = 'AI Architect & CTO | Jacek Janczura';
const description =
  'Co-founder & CTO at Jaden Data. Transforming enterprise operations through process intelligence—AI-native systems that turn operational complexity into competitive advantage across manufacturing, pharma, VC, real estate, and finance.';
const lang = 'en';
---

<BaseLayout title={title} description={description} lang={lang}>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-background">
      <div class="geometric-shape shape-1"></div>
      <div class="geometric-shape shape-2"></div>
      <div class="geometric-shape shape-3"></div>
      <div class="geometric-shape shape-4"></div>
      <div class="geometric-shape shape-5"></div>
      <div class="geometric-shape shape-6"></div>
    </div>
    <div class="container">
      <h1 class="hero-title">AI Architect & CTO</h1>
      <p class="hero-subtitle">
        Transforming operational complexity into competitive advantage
      </p>
      <p class="hero-tagline">
        Co-founder at Jaden Data — Building process intelligence for the AI era
      </p>
      <div class="hero-cta">
        <a href="/en/contact/" class="cta-button">Get in touch</a>
        <a href="/en/projects/" class="cta-link">View projects</a>
      </div>
    </div>
  </section>

  <!-- Positioning Section -->
  <section class="positioning">
    <div class="container content">
      <p>
        I'm the Co-founder & CTO of <strong>Jaden Data</strong>, where we
        architect process intelligence—AI-native systems that transform how
        businesses operate. We don't just build AI tools; we start with
        understanding how your business actually works, then enhance it with
        intelligent automation.
      </p>
      <p>
        My expertise spans <strong
          >enterprise LLM platforms, RAG systems, and secure cloud architectures</strong
        >, but it's applied through a process-first lens. I design <strong
          >event-driven microservices</strong
        > on AWS, integrate multi-model stacks (OpenAI, Azure, Bedrock, Mistral),
        and deliver
        <strong>ISO 27001/SOC 2</strong> compliance—all to transform operations across
        manufacturing, pharmaceutical, venture capital, real estate, and financial
        services. The goal: measurable business impact, not just technical sophistication.
      </p>
    </div>
  </section>

  <!-- Highlights Section -->
  <section class="highlights">
    <div class="container">
      <h2>Where Process Intelligence Meets Technical Excellence</h2>
      <div class="highlights-grid">
        <div class="highlight-card">
          <h3>Enterprise AI Platforms</h3>
          <p>
            Multi-tenant platforms serving 50+ organizations with &gt;1,000
            concurrent connections. Built for mission-critical operations with
            ISO 27001/SOC 2 compliance delivered in ~3 months.
          </p>
        </div>
        <div class="highlight-card">
          <h3>Process-First AI Design</h3>
          <p>
            Starting with how businesses actually operate, then enhancing
            workflows with RAG systems, knowledge-aware agents, and intelligent
            automation that serves the process, not replaces it.
          </p>
        </div>
        <div class="highlight-card">
          <h3>Industry-Specific Solutions</h3>
          <p>
            Real estate valuation saving 2+ hours per appraisal. VC due
            diligence automation. Manufacturing quality control. Pharma
            compliance workflows. Solutions built for specific operational
            challenges.
          </p>
        </div>
        <div class="highlight-card">
          <h3>Security & Compliance</h3>
          <p>
            Enterprise-grade security isn't an afterthought—it's fundamental.
            ISO 27001 and SOC 2 certified platforms with complete data
            sovereignty for industries that can't compromise.
          </p>
        </div>
        <div class="highlight-card">
          <h3>Measurable Business Impact</h3>
          <p>
            From implementation to results in weeks, not months. Pre-built
            platforms for speed, custom solutions for perfect fit. All designed
            to deliver ROI you can measure and scale.
          </p>
        </div>
        <div class="highlight-card">
          <h3>Distributed Systems at Scale</h3>
          <p>
            Event-driven microservices on AWS handling thousands of RPS with
            high-throughput design and controlled costs. Technology that
            performs under load while delivering business value.
          </p>
        </div>
      </div>
      <p class="highlights-note">
        Certified skydiver and diver; wind/kitesurf instructor; snowboarder.
        These experiences build risk management, clear instruction, calm under
        pressure, and disciplined preparation—skills that translate directly to
        technical leadership and building systems that businesses depend on.
      </p>
    </div>
  </section>

  <!-- Featured Work Section -->
  <section class="featured-work">
    <div class="container">
      <h2>Process Intelligence in Action</h2>
      <div class="featured-grid">
        <a href="/en/projects/" class="featured-item">
          <h3>entAIngine</h3>
          <p>
            Enterprise AI platform transforming mission-critical back office
            operations for production facilities, pharma companies, and
            financial institutions. ISO 27001/SOC 2 certified with complete data
            sovereignty.
          </p>
          <span class="featured-link">View all projects →</span>
        </a>
        <a href="/en/projects/" class="featured-item">
          <h3>Valuation Engine</h3>
          <p>
            Real estate appraisal platform saving 2+ hours per report.
            Transforms hours of manual research into minutes of actionable
            intelligence—process intelligence applied to property valuation.
          </p>
          <span class="featured-link">View all projects →</span>
        </a>
        <a href="/en/projects/" class="featured-item">
          <h3>Flowhive VC</h3>
          <p>
            Venture capital platform combining deep research with
            investment-specific automation. Accelerates due diligence, provides
            AI-driven portfolio insights, and transforms tribal knowledge into
            institutional intelligence.
          </p>
          <span class="featured-link">View all projects →</span>
        </a>
        <a href="/en/talks/" class="featured-item">
          <h3>AI Architecture Patterns</h3>
          <p>
            Practical lessons from enterprise LLM deployments—how to architect
            systems that serve business processes, not just implement
            technology.
          </p>
          <span class="featured-link">Watch talks →</span>
        </a>
      </div>
    </div>
  </section>

  <!-- Contact CTA Section -->
  <section class="contact-cta">
    <div class="container content">
      <h2>Ready to Transform Your Operations?</h2>
      <p>
        Whether you're looking to implement proven AI platforms or need custom
        solutions for unique challenges, let's start with a conversation about
        your processes. Best via email or LinkedIn. I'm open to advisory,
        architecture reviews, and select delivery engagements.
      </p>
      <a href="/en/contact/" class="cta-button">Get in touch</a>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    padding: var(--space-8) 0;
    text-align: center;
    background: linear-gradient(
      135deg,
      var(--bg) 0%,
      oklch(from var(--primary) l c h / 0.05) 30%,
      oklch(from var(--accent) l c h / 0.03) 60%,
      var(--muted) 100%
    );
    position: relative;
    overflow: hidden;
  }

  /* Additional gradient overlay for depth */
  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(
      ellipse at top right,
      oklch(from var(--primary) l c h / 0.08) 0%,
      transparent 50%
    );
    pointer-events: none;
    z-index: 0;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 0;
    opacity: 0.15;
  }

  .geometric-shape {
    position: absolute;
    border: 2px solid var(--primary);
    opacity: 0;
    animation-timing-function: ease-in-out;
  }

  /* Hexagon shape */
  .shape-1 {
    width: 120px;
    height: 120px;
    top: 10%;
    left: 10%;
    clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
    animation: randomMove1 8s infinite;
    animation-delay: 0s;
  }

  /* Triangle shape */
  .shape-2 {
    width: 80px;
    height: 80px;
    top: 60%;
    right: 15%;
    clip-path: polygon(50% 0%, 0% 100%, 100% 100%);
    animation: randomMove2 11s infinite;
    animation-delay: 2s;
  }

  /* Square rotated */
  .shape-3 {
    width: 100px;
    height: 100px;
    top: 30%;
    right: 25%;
    border-radius: var(--radius-m);
    animation: randomMove3 9s infinite;
    animation-delay: 4s;
  }

  /* Circle */
  .shape-4 {
    width: 90px;
    height: 90px;
    bottom: 20%;
    left: 20%;
    border-radius: 50%;
    animation: randomMove4 13s infinite;
    animation-delay: 1s;
  }

  /* Diamond */
  .shape-5 {
    width: 70px;
    height: 70px;
    top: 5%;
    right: 10%;
    clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
    animation: randomMove5 10s infinite;
    animation-delay: 3s;
  }

  /* Octagon */
  .shape-6 {
    width: 85px;
    height: 85px;
    bottom: 10%;
    right: 30%;
    clip-path: polygon(
      30% 0%,
      70% 0%,
      100% 30%,
      100% 70%,
      70% 100%,
      30% 100%,
      0% 70%,
      0% 30%
    );
    animation: randomMove6 12s infinite;
    animation-delay: 5s;
  }

  @keyframes randomMove1 {
    0% {
      opacity: 0;
      transform: translate(0, 0) rotate(0deg) scale(0.8);
    }
    15% {
      opacity: 0.5;
      transform: translate(40px, -50px) rotate(120deg) scale(1);
    }
    30% {
      opacity: 0.3;
      transform: translate(-30px, 60px) rotate(240deg) scale(0.9);
    }
    45% {
      opacity: 0.6;
      transform: translate(60px, 30px) rotate(360deg) scale(1.1);
    }
    60% {
      opacity: 0.2;
      transform: translate(-20px, -40px) rotate(480deg) scale(0.85);
    }
    75% {
      opacity: 0.4;
      transform: translate(50px, 70px) rotate(600deg) scale(1);
    }
    90% {
      opacity: 0.3;
      transform: translate(-40px, 20px) rotate(720deg) scale(0.95);
    }
    100% {
      opacity: 0;
      transform: translate(0, 0) rotate(840deg) scale(0.8);
    }
  }

  @keyframes randomMove2 {
    0% {
      opacity: 0;
      transform: translate(0, 0) rotate(0deg) scale(0.7);
    }
    20% {
      opacity: 0.4;
      transform: translate(-50px, 40px) rotate(180deg) scale(1);
    }
    35% {
      opacity: 0.6;
      transform: translate(70px, -30px) rotate(360deg) scale(1.2);
    }
    50% {
      opacity: 0.2;
      transform: translate(-40px, 50px) rotate(540deg) scale(0.8);
    }
    65% {
      opacity: 0.5;
      transform: translate(30px, -60px) rotate(720deg) scale(1.1);
    }
    80% {
      opacity: 0.3;
      transform: translate(-60px, 30px) rotate(900deg) scale(0.9);
    }
    100% {
      opacity: 0;
      transform: translate(0, 0) rotate(1080deg) scale(0.7);
    }
  }

  @keyframes randomMove3 {
    0% {
      opacity: 0;
      transform: translate(0, 0) rotate(0deg) scale(0.9);
    }
    18% {
      opacity: 0.5;
      transform: translate(50px, 60px) rotate(135deg) scale(1.1);
    }
    32% {
      opacity: 0.3;
      transform: translate(-60px, -40px) rotate(270deg) scale(0.85);
    }
    48% {
      opacity: 0.6;
      transform: translate(40px, -50px) rotate(405deg) scale(1.2);
    }
    64% {
      opacity: 0.2;
      transform: translate(-30px, 70px) rotate(540deg) scale(0.9);
    }
    80% {
      opacity: 0.4;
      transform: translate(70px, 20px) rotate(675deg) scale(1);
    }
    100% {
      opacity: 0;
      transform: translate(0, 0) rotate(810deg) scale(0.9);
    }
  }

  @keyframes randomMove4 {
    0% {
      opacity: 0;
      transform: translate(0, 0) scale(0.8);
    }
    12% {
      opacity: 0.4;
      transform: translate(-40px, -60px) scale(1);
    }
    28% {
      opacity: 0.6;
      transform: translate(60px, 40px) scale(1.15);
    }
    42% {
      opacity: 0.2;
      transform: translate(-50px, 50px) scale(0.85);
    }
    58% {
      opacity: 0.5;
      transform: translate(50px, -30px) scale(1.05);
    }
    72% {
      opacity: 0.3;
      transform: translate(-30px, -50px) scale(0.95);
    }
    88% {
      opacity: 0.4;
      transform: translate(40px, 60px) scale(1.1);
    }
    100% {
      opacity: 0;
      transform: translate(0, 0) scale(0.8);
    }
  }

  @keyframes randomMove5 {
    0% {
      opacity: 0;
      transform: translate(0, 0) rotate(0deg) scale(0.75);
    }
    22% {
      opacity: 0.5;
      transform: translate(60px, -40px) rotate(180deg) scale(1.1);
    }
    38% {
      opacity: 0.3;
      transform: translate(-50px, 60px) rotate(360deg) scale(0.9);
    }
    54% {
      opacity: 0.6;
      transform: translate(40px, 50px) rotate(540deg) scale(1.2);
    }
    70% {
      opacity: 0.2;
      transform: translate(-60px, -30px) rotate(720deg) scale(0.8);
    }
    86% {
      opacity: 0.4;
      transform: translate(30px, -50px) rotate(900deg) scale(1);
    }
    100% {
      opacity: 0;
      transform: translate(0, 0) rotate(1080deg) scale(0.75);
    }
  }

  @keyframes randomMove6 {
    0% {
      opacity: 0;
      transform: translate(0, 0) rotate(0deg) scale(0.85);
    }
    16% {
      opacity: 0.4;
      transform: translate(-70px, 30px) rotate(90deg) scale(1);
    }
    33% {
      opacity: 0.6;
      transform: translate(50px, -50px) rotate(180deg) scale(1.15);
    }
    49% {
      opacity: 0.2;
      transform: translate(-40px, 70px) rotate(270deg) scale(0.9);
    }
    65% {
      opacity: 0.5;
      transform: translate(60px, 40px) rotate(360deg) scale(1.05);
    }
    81% {
      opacity: 0.3;
      transform: translate(-30px, -60px) rotate(450deg) scale(0.95);
    }
    100% {
      opacity: 0;
      transform: translate(0, 0) rotate(540deg) scale(0.85);
    }
  }

  /* Dark theme adjustments */
  :root[data-theme='dark'] .hero-background {
    opacity: 0.2;
  }

  :root[data-theme='dark'] .geometric-shape {
    opacity: 0.5;
  }

  /* Respect reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .geometric-shape {
      animation: none;
    }
  }

  .hero .container {
    position: relative;
    z-index: 1;
  }

  .hero-title {
    font-size: var(--font-size-hero);
    margin-bottom: var(--space-3);
    background: linear-gradient(
      135deg,
      var(--text) 0%,
      var(--primary) 50%,
      var(--text) 100%
    );
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 200% auto;
    animation: gradient-shift 8s ease infinite;
  }

  @keyframes gradient-shift {
    0%,
    100% {
      background-position: 0% center;
    }
    50% {
      background-position: 100% center;
    }
  }

  .hero-subtitle {
    font-size: var(--font-size-xl);
    background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: var(--space-2);
    font-weight: var(--font-weight-bold);
  }

  .hero-tagline {
    font-size: var(--font-size-lg);
    color: var(--text);
    opacity: 0.8;
    margin-bottom: var(--space-6);
    font-weight: var(--font-weight-normal);
    font-style: italic;
  }

  .hero-cta {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    flex-wrap: wrap;
  }

  .cta-button {
    background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
    color: white;
    padding: var(--space-3) var(--space-6);
    border-radius: var(--radius-m);
    text-decoration: none;
    font-weight: var(--font-weight-bold);
    transition: all 0.3s ease;
    box-shadow: 0 4px 12px oklch(from var(--primary) l c h / 0.3);
    position: relative;
    overflow: hidden;
  }

  .cta-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.2),
      transparent
    );
    transition: left 0.5s ease;
  }

  .cta-button:hover {
    background: linear-gradient(135deg, var(--accent) 0%, var(--primary) 100%);
    text-decoration: none;
    transform: translateY(-2px);
    box-shadow: 0 6px 16px oklch(from var(--primary) l c h / 0.4);
  }

  .cta-button:hover::before {
    left: 100%;
  }

  .cta-button:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }

  .cta-link {
    color: var(--primary);
    padding: var(--space-3) var(--space-6);
    border: 1px solid var(--primary);
    border-radius: var(--radius-m);
    text-decoration: none;
    transition: all 0.2s;
  }

  .cta-link:hover {
    background: var(--primary);
    color: white;
    text-decoration: none;
  }

  .cta-link:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }

  .positioning {
    padding: var(--space-8) 0;
    background: linear-gradient(
      180deg,
      var(--bg) 0%,
      oklch(from var(--muted) l c h / 0.5) 100%
    );
    position: relative;
  }

  .positioning::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--border), transparent);
  }

  .highlights {
    padding: var(--space-8) 0;
    background: linear-gradient(
      135deg,
      var(--muted) 0%,
      oklch(from var(--primary) l c h / 0.02) 50%,
      var(--muted) 100%
    );
    position: relative;
  }

  .highlights::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(
      90deg,
      transparent,
      var(--primary),
      transparent
    );
    opacity: 0.3;
  }

  .highlights h2 {
    text-align: center;
    margin-bottom: var(--space-6);
    background: linear-gradient(135deg, var(--text) 0%, var(--primary) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .highlights-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-5);
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .highlight-card {
    background: linear-gradient(
      135deg,
      var(--bg) 0%,
      oklch(from var(--primary) l c h / 0.02) 100%
    );
    padding: var(--space-5);
    border-radius: var(--radius-l);
    box-shadow: var(--shadow-1);
    text-align: center;
    transition: all 0.3s ease;
    border: 1px solid transparent;
    position: relative;
    overflow: hidden;
  }

  .highlight-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(
      90deg,
      var(--primary),
      var(--accent),
      var(--primary)
    );
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .highlight-card:hover {
    transform: translateY(-4px);
    border-color: oklch(from var(--primary) l c h / 0.2);
    box-shadow: var(--shadow-2);
    background: linear-gradient(
      135deg,
      var(--bg) 0%,
      oklch(from var(--primary) l c h / 0.05) 100%
    );
  }

  .highlight-card:hover::before {
    opacity: 1;
  }

  .highlight-card h3 {
    background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: var(--space-3);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-bold);
  }

  .highlights-note {
    text-align: center;
    font-size: var(--font-size-sm);
    color: var(--text);
    opacity: 0.8;
    margin-top: var(--space-4);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .featured-work {
    padding: var(--space-8) 0;
    background: linear-gradient(
      180deg,
      var(--bg) 0%,
      oklch(from var(--muted) l c h / 0.3) 100%
    );
  }

  .featured-work h2 {
    text-align: center;
    margin-bottom: var(--space-6);
    background: linear-gradient(135deg, var(--text) 0%, var(--primary) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .featured-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-6);
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .featured-item {
    display: block;
    padding: var(--space-5);
    border: 1px solid var(--border);
    border-radius: var(--radius-l);
    transition: all 0.3s ease;
    background: linear-gradient(
      135deg,
      var(--bg) 0%,
      oklch(from var(--primary) l c h / 0.01) 100%
    );
    position: relative;
    overflow: hidden;
    text-decoration: none;
    color: inherit;
  }

  .featured-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      135deg,
      oklch(from var(--primary) l c h / 0.05) 0%,
      transparent 100%
    );
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .featured-item:hover {
    border-color: var(--primary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-2);
    text-decoration: none;
  }

  .featured-item:hover::before {
    opacity: 1;
  }

  .featured-item:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }

  .featured-item h3 {
    background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: var(--space-3);
    font-size: var(--font-size-xl);
    position: relative;
    z-index: 1;
  }

  .featured-link {
    color: var(--primary);
    font-weight: var(--font-weight-bold);
    margin-top: var(--space-3);
    display: inline-block;
    transition: transform 0.2s ease;
  }

  .featured-item:hover .featured-link {
    transform: translateX(4px);
  }

  .contact-cta {
    padding: var(--space-8) 0;
    background: linear-gradient(
      135deg,
      var(--muted) 0%,
      oklch(from var(--primary) l c h / 0.03) 50%,
      var(--muted) 100%
    );
    text-align: center;
    position: relative;
  }

  .contact-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(
      90deg,
      transparent,
      var(--primary),
      var(--accent),
      var(--primary),
      transparent
    );
    opacity: 0.4;
  }

  .contact-cta h2 {
    margin-bottom: var(--space-3);
    background: linear-gradient(135deg, var(--text) 0%, var(--primary) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .contact-cta p {
    margin-bottom: var(--space-5);
    font-size: var(--font-size-lg);
  }

  @media (max-width: 600px) {
    .hero-cta {
      flex-direction: column;
      align-items: center;
    }

    .highlights-grid {
      grid-template-columns: 1fr;
    }

    .featured-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
