---
import BaseLayout from '../../layouts/BaseLayout.astro';

const title = 'Kontakt | Jacek Janczura';
const description =
  'Kontakt w sprawie przeglądów architektury AI, doradztwa i projektów deweloperskich. Dostępny dla projektowania platform AI dla przedsiębiorstw i przywództwa technicznego.';
const lang = 'pl';
---

<BaseLayout title={title} description={description} lang={lang}>
  <section class="contact-hero">
    <div class="container">
      <h1>Kontakt</h1>
      <p class="contact-subtitle">Budujmy odpowiedzialnie projektowane AI</p>
    </div>
  </section>

  <section class="contact-content">
    <div class="container content">
      <div class="contact-intro">
        <p>
          Najlepiej przez formularz poniżej lub przez LinkedIn. Jestem otwarty na
          doradztwo, przeglądy architektury i wybrane projekty delivery skupione
          na platformach AI dla przedsiębiorstw.
        </p>
      </div>

      <div class="contact-methods">
        <div class="contact-method">
          <h2>Formularz kontaktowy</h2>
          <p>
            Dla zapytań zawodowych, dyskusji projektowych i możliwości współpracy.
          </p>
          <form
            action="https://formspree.io/f/myzngpqa"
            method="POST"
            class="contact-form"
          >
            <label class="form-label">
              Twój e‑mail:
              <input type="email" name="email" class="form-input" required />
            </label>
            <label class="form-label">
              Twoja wiadomość:
              <textarea name="message" class="form-textarea" rows="4" required
              ></textarea>
            </label>
            <button type="submit" class="form-submit">Wyślij wiadomość</button>
          </form>
        </div>

        <div class="contact-method">
          <h2>LinkedIn</h2>
          <p>Dla networkingu, dyskusji branżowych i kontaktów zawodowych.</p>
          <a
            href="https://linkedin.com/in/jacekjanczura"
            target="_blank"
            rel="noopener noreferrer"
            class="contact-link"
          >
            linkedin.com/in/jacekjanczura
          </a>
        </div>

        <div class="contact-method">
          <h2>GitHub</h2>
          <p>Dla wkładów open source, przeglądów kodu i dyskusji technicznych.</p>
          <a
            href="https://github.com/jacekjanczura"
            target="_blank"
            rel="noopener noreferrer"
            class="contact-link"
          >
            github.com/jacekjanczura
          </a>
        </div>
      </div>

      <div class="contact-focus">
        <h2>Obszary specjalizacji</h2>
        <div class="focus-grid">
          <div class="focus-item">
            <h3>Architektura platform AI</h3>
            <p>
              Platformy LLM wielodostępne, systemy RAG i projektowanie potoków AI
            </p>
          </div>
          <div class="focus-item">
            <h3>Przywództwo techniczne</h3>
            <p>
              Budowanie zespołów, kultura przeglądów kodu i optymalizacja procesu
              dostarczania
            </p>
          </div>
          <div class="focus-item">
            <h3>Bezpieczeństwo i compliance</h3>
            <p>Wdrażanie ISO 27001/SOC 2 i ocena ryzyka specyficznego dla AI</p>
          </div>
          <div class="focus-item">
            <h3>Projektowanie systemów</h3>
            <p>
              Mikrousługi sterowane zdarzeniami, systemy rozproszone i
              optymalizacja wydajności
            </p>
          </div>
        </div>
      </div>

      <div class="contact-cta">
        <p>
          Współpracuję z organizacjami budującymi rozwiązania AI dla przedsiębiorstw,
          które wymagają solidnej architektury, projektowania z bezpieczeństwem na
          pierwszym miejscu i skalowalnej implementacji.
        </p>
        <p>
          Obecnie skupiam się na rozwoju platform LLM, Prompt Engineering w skali
          i frameworkach zarządzania AI.
        </p>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .contact-hero {
    padding: var(--space-8) 0;
    background: linear-gradient(135deg, var(--bg) 0%, var(--muted) 100%);
    text-align: center;
  }

  .contact-hero h1 {
    font-size: var(--font-size-hero);
    margin-bottom: var(--space-3);
    color: var(--text);
  }

  .contact-subtitle {
    font-size: var(--font-size-xl);
    color: var(--primary);
    margin-bottom: 0;
    font-weight: var(--font-weight-normal);
    max-width: var(--content-width);
    margin-left: auto;
    margin-right: auto;
  }

  .contact-content {
    padding: var(--space-8) 0;
    background: var(--bg);
  }

  .contact-intro {
    text-align: center;
    margin-bottom: var(--space-8);
  }

  .contact-intro p {
    font-size: var(--font-size-lg);
    line-height: 1.6;
    margin-bottom: 0;
  }

  .contact-methods {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-6);
    margin-bottom: var(--space-8);
  }

  .contact-method {
    padding: var(--space-5);
    border: 1px solid var(--border);
    border-radius: var(--radius-l);
    transition: border-color 0.2s;
  }

  .contact-method:hover {
    border-color: var(--primary);
  }

  .contact-method h2 {
    font-size: var(--font-size-xl);
    color: var(--primary);
    margin-bottom: var(--space-3);
  }

  .contact-method p {
    margin-bottom: var(--space-3);
    color: var(--text);
    opacity: 0.8;
  }

  .contact-link {
    color: var(--primary);
    text-decoration: none;
    font-weight: var(--font-weight-bold);
    font-size: var(--font-size-lg);
    display: inline-block;
    transition: color 0.2s;
  }

  .contact-link:hover {
    color: var(--accent);
    text-decoration: none;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .form-label {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-bold);
    color: var(--text);
  }

  .form-input,
  .form-textarea {
    padding: var(--space-3);
    border: 1px solid var(--border);
    border-radius: var(--radius-m);
    font-family: var(--font-body);
    font-size: var(--font-size-base);
    background: var(--bg);
    color: var(--text);
    transition: border-color 0.2s;
  }

  .form-input:focus,
  .form-textarea:focus {
    outline: none;
    border-color: var(--primary);
  }

  .form-submit {
    padding: var(--space-3) var(--space-4);
    background: var(--primary);
    color: var(--bg);
    border: none;
    border-radius: var(--radius-m);
    font-family: var(--font-body);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-bold);
    cursor: pointer;
    transition: background-color 0.2s;
  }

  .form-submit:hover {
    background: var(--accent);
  }

  .contact-focus {
    margin-bottom: var(--space-8);
  }

  .contact-focus h2 {
    text-align: center;
    margin-bottom: var(--space-6);
    color: var(--primary);
  }

  .focus-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-4);
  }

  .focus-item {
    text-align: center;
    padding: var(--space-4);
    background: var(--muted);
    border-radius: var(--radius-m);
  }

  .focus-item h3 {
    font-size: var(--font-size-lg);
    color: var(--primary);
    margin-bottom: var(--space-2);
  }

  .focus-item p {
    font-size: var(--font-size-sm);
    margin: 0;
    line-height: 1.5;
  }

  .contact-cta {
    background: var(--muted);
    padding: var(--space-6);
    border-radius: var(--radius-l);
    text-align: center;
  }

  .contact-cta p {
    margin-bottom: var(--space-3);
    font-size: var(--font-size-lg);
    line-height: 1.6;
  }

  .contact-cta p:last-child {
    margin-bottom: 0;
  }

  @media (max-width: 600px) {
    .contact-methods {
      grid-template-columns: 1fr;
    }

    .focus-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
